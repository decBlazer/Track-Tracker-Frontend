import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Use ReactDOM.createRoot if you are using React 18 or later
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);